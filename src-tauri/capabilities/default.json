{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认能力：核心事件（listen/emit 给原始输入与焦点事件用）。自定义命令不需要在这里声明权限。",
  "windows": ["main"],
  "permissions": ["core:default"]
}
