{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability: core events (listen/emit, used by raw input and the focus events). Custom commands do not need a permission declared here.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
